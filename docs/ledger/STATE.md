# STATE

The handoff file. Both spaces write it. Every open item names the side that owns
it and the state it is in — if an item has no owner, nobody is doing it and both
spaces think the other one is.

Last touched by: **decision** · 2026-09-19

## Authorized now

- ruling: 0029
- also-in-force: ruling 0030 (item 53's authorization)
- work: item 52 — Slice 8 email send under ruling 0029's five clauses; item
  53 — the Research tab's second ledger under ruling 0030. Both Phase 1 of
  decision 0007, the owner's pathway to the live product, deadline Saturday
  2026-09-26.
- the week's shape (decision 0007): Phase 1 closes the find→strategize→
  draft→send loop; then proposals and a v1 deck; nurture v1 through the
  send gate; network-outreach v1 as manual capture only; monetization
  (provisional schedule from ledger data, tiers, founding-tester terms) in
  parallel; the 0025-clause-4 experiment once the schedule exists. What
  Saturday excludes is named in the decision.
- boundaries: hard rule 1 is now ruled construction (0029) — no batch, no
  schedule, no trigger, no retry, one module, one importer. Testers are
  PAUSED by the owner (2026-09-21) until the Saturday build-out completes —
  no live-user risk mid-week, main still stays deployable. Consequence: the
  provisional credit schedule cannot come from tester usage; it comes from
  our own measured runs, labelled as such (ruling 0021), and gets re-set
  from real usage after testers resume.

## Next authorized

Nothing queued behind item 39. What moves next is decision-owned: item 28
wants a ruling (the quoted-vs-asserted vocabulary, three live examples), item 30 waits
on the owner running the paid half of the measurement — the only route to
absence precision — and items 25, 34, 26/35/20 wait on the owner's product
judgment, not on analysis.

## Open items

| id | owner | status | subject | opened |
|----|-------|--------|---------|--------|
| 54 | decision | proposed | **Two grant-size facts fall through the availability engine's fallback.** From item 53's escalation: `funding.grant_size_range` and `funding.median_grant_size` have no KEY_PURPOSE or KEY_SOURCE entry in `lib/availability.ts`, so their reason renders as a terse "not retrieved" and their source defaults to official_site even though GRANT_SCHEDULE_KEYS treats them as 990 grant-schedule facts — a read filing never settles them. Errs toward staying open (safe direction), but the stated source is wrong and the wording is the worst on the ledger. Fix is two map entries — an engine change ruling 0030 did not authorize. Small; fold into the next availability-engine authorization. | 2026-09-21 |
| 53 | build | released | **Released 2026-09-21 — pushed with the countercheck commit; display only, no migration dependency.** Counterchecked against the diffs: 143 insertions, 0 deletions; the screening ledger byte-identical; the second `availabilityForResearchRun` call takes `requiredClaimKeysFor("strategy")` — derived from the policy source, not hard-coded — and feeds display only, the ruling-0009 offer still reading screening alone; the strategy-minus-screening difference is exactly the 8 ruled keys, asserted in `scripts/test-availability.ts` 69 → 84 of 84. Escalation recorded as item 54. Original authorization: **ruling 0030 (scope). Phase 1 of decision 0007.** The Research tab carries a second, strategy-scoped availability ledger beside the unchanged screening one: strategy's required set including its 8 extra keys, each displayed with availability and reason (ruling 0017), wired per ruling 0011's existing consumer pattern (`availabilityForResearchRun` as the model). Display only; no consumer's `keys` set changes; shared UI tokens. Acceptance: the 8 keys visible with reasons on a prospect's Research tab; the screening ledger unchanged; `npx tsc --noEmit` clean; `npx next build` compiles; touched tests passing with counts. | 2026-09-21 |
| 58 | decision | proposed | **Per-org verified sending domains — the end state of email identity.** Raised by the owner 2026-09-21: a from-address must not be platform-hardwired. The full answer is each organization verifying its own domain with the provider (Resend Domains API) and sending as itself, with the platform pattern as fallback until verified. An onboarding feature: DNS records, verification states, a stored org-level from-address. Post-Saturday; decide with the onboarding/monetization work. | 2026-09-21 |
| 59 | build | approved | **Authorized 2026-09-21 — the attempt ledger captures the identity it sent (ruling 0029 clause 4 extended).** From item 57's escalation: an org can rename itself after a send, so the historical from-identity is unreconstructible unless captured at birth. One additive migration (next free number): nullable `from_identity` and `reply_to` on `draft_send_attempts`, written at birth with the rest of the payload; the birth-pinning trigger extends to them; `buildInteractionSummary` unchanged unless trivial. Safe both deploy orders (columns nullable; capture code tolerates their absence — or fails closed with the birth insert, state which). Tests extended; SQL delivered verbatim. Small — do it before the first real send so history is complete from send one. | 2026-09-21 |
| 57 | build | released | **Released 2026-09-21 — counterchecked: from is "{org name}" <platform address> built at `lib/draft-send.ts:170`, replyTo the clicking user's email, both consumed verbatim by the provider module from the same payload the dialog displays; three refusal codes replace any silent fallback; `scripts/test-send-draft.ts` 52 → 67 offline of 67; five-clause machinery byte-unchanged per the split env scan. Escalation became item 59: the attempt ledger must capture the identity it sent. Original: **owner-raised, no user identity hardwired.** The platform env address remains infrastructure only. The email presents as the ORGANIZATION: friendly-from "{org display name}" <platform address>, name derived from the caller's organization record, never typed. reply_to is the email of the human who clicked send, so a funder's reply reaches the right person at the right org. Both fields flow through `evaluateSendReadiness` so the confirmation displays exactly the identity and reply-to that will be sent (ruling 0029 clause 2's confirmation-equals-payload holds for the new fields too). Scope: `lib/draft-send.ts`, `lib/send-draft.ts`, the handler's payload assembly, the confirmation dialog, `scripts/test-send-draft.ts` updated. No change to the five-clause machinery: same one module, one importer, birth-before-provider, once-only. | 2026-09-21 |
| 56 | decision | proposed | **A prospect with send history is undeletable, and the delete button does not know it.** Deliberate consequence of migration 0069 (the attempt FK has no cascade — a record of mail that reached a funder cannot be deleted out from under itself). The existing prospect delete flow would surface a raw FK error. Wants a decided UX: hide delete, or explain retention. Post-Saturday unless a tester hits it. | 2026-09-21 |
| 55 | decision | proposed | **Drafts governance gaps, pre-existing, surfaced by item 52.** (a) `drafts` is not covered by `scripts/test-tenant-isolation.ts` — the only check on hard rule 6 has never covered the table that now carries send facts. (b) `updateDraft`/`approveDraft` have no server-side status guard (UI-only); the 0069 trigger now hard-blocks post-send edits at the database, but through the old actions that surfaces as a production-redacted thrown error. (c) Older test tolerances accept only 42P01 for a missing table while PostgREST actually reports PGRST205 (observed live). Three small fixes wanting one authorization. | 2026-09-21 |
| 52 | build | released | **Released 2026-09-21 — origin main at 8981c1b; deploys safely ahead of its migration because the code fails closed (the birth insert precedes the provider call, so with no attempt table nothing can send — verified in the handler, not the report). Awaiting: owner applies migration 0069, sets RESEND_FROM_EMAIL in Vercel, then the 19+8 waiting DB assertions and a real send to the owner's own address.** Counterchecked hardest of the week: exactly one provider SDK import in the codebase (`lib/send-draft.ts`), one importer; birth-before-provider ordering read in `app/(dashboard)/prospects/[id]/send-actions.ts`; sent facts mirror a confirmed attempt and are immutable after (two triggers, two partial unique indexes); attempted-unconfirmed is the absence of an outcome; `scripts/test-send-draft.ts` 52 of 52 offline, closed-set scan proven by rogue-call experiment (52→49 and 52→51, restored 52); `npx tsc --noEmit` exit 0; `npx next build` compiles. Note: `lib/invite.ts` contains no Resend at all — the funder-facing module is the codebase's ONLY provider usage, and the CLAUDE.md annex claim that invites use Resend is stale. Spec drift recorded in the slice doc's As-built note. Escalations: items 55 and 56. Original authorization: **ruling 0029 (scope). Phase 1 of decision 0007.** Build per `docs/slices/slice-08-email-send.md` under the five clauses: one additive migration (nullable sent_at, sent_by, provider message id on drafts, plus DB enforcement that sent facts are written once — the 0067 terminal-once pattern); ONE server-side funder-facing send module (Resend) imported by ONE handler that takes an approved draft id and re-verifies exists/org/approved/never-sent server-side at send time; confirmation UI on approved drafts showing exact recipient, subject, body; automatic interaction log from the same payload sent; attempted-unconfirmed recorded when the provider response never arrives; a closed-set scan test failing on any second send call or importer (the `scripts/test-ai-runs.ts` pattern); tests per clause. The invite path untouched. NOT authorized: batch, scheduling, retries, templates. Migration SQL user-applied per standing preference; note the deploy-order in the report if the code refuses to run pre-migration. | 2026-09-21 |
| 51 | decision | proposed | **Outcome display beyond the ruled minimum — deferred past Monday, cosmetic.** From item 49's escalations: a closed prospect's board card can show a "Stalled" health chip beside "Declined · Never revisit" (health is a date fact, not a work offer, so ruling 0028 permits it — but the juxtaposition reads oddly); the pipeline stage drill-down and List view show no outcome (0028 names board + prospect page as the minimum). Decide the full display story alongside items 26/20. | 2026-09-19 |
| 50 | decision | proposed | **A second outcome can be recorded while one is in effect.** Pre-existing, surfaced by item 49: recordProspectDecline does not check for an outcome already in effect, so a prospect can carry two live declines; the derivation picks the most recent, so nothing breaks, but whether the second recording should be refused, allowed, or merge-prompted is direction. Post-Monday. | 2026-09-19 |
| 49 | build | released | **Released 2026-09-21 — origin main at d396e23; migration 0068 applied by the owner and verified here by direct REST probe. First user-facing behaviour change since item 23. DB tests executed by the owner 2026-09-21: `scripts/test-tenant-isolation.ts` 41 passed 0 failed over both new tables — retraction append-only proven (no update, no delete, second retraction refused, both rows readable after retraction). Smoke-tested by the owner in the deployed app 2026-09-21 and verified here by direct probe: a decline recorded and retracted (both rows present, retraction naming its outcome and note), and the run ledger's first live row — revisit_suggest, born 02:02:42Z, finalized completed 6s later, model and 2,026/230 tokens captured from the response. MVP is tester-ready.** Counterchecked in the decision space against the diffs: the derivation is one function pair (`isOutcomeInEffect`/`latestOutcomeInEffect` in `lib/prospect-outcomes.ts`) and a repo-wide scan test asserts exactly two modules touch the retraction table and all four work surfaces reference `isClosedToWork`; the retraction action is an explicit human insert with the unique violation answered in plain words; work lists exclude effective-`never` while the board card and prospect page show the outcome with its reason. `scripts/test-prospect-outcomes.ts` 121 → 176 of 176; `npx tsc --noEmit` exit 0; `npx next build` compiles, 28 routes; degrades safely pre-migration (verified against the running app with 0068 absent). **Ratified in countercheck:** effective = most recent non-retracted outcome (retracting the newest reveals an older unretracted one); the sidebar badge counts what its tab lists; dashboard priorities got only the `never` exclusion. Escalations recorded as items 50 and 51. Review: `docs/reviews/0021-an-outcome-can-be-taken-back.md`. Original authorization: **rulings 0027 and 0028 (scopes).** Blocked from dispatch until item 48 lands (both touch `scripts/test-tenant-isolation.ts` and the revisit surfaces). One additive migration 0068: prospect outcome retractions — org-scoped per hard rule 6, 0066 pattern, org-match trigger on the outcome reference, append-only, no delete/update policies. `lib/prospect-outcomes.ts`: one effective-outcome derivation (outcome unless retracted), every consumer reads it. A human retraction action where outcomes are shown. Due-now/follow-up queries exclude effective-`never` prospects; Pipeline board and prospect page show the outcome with its reason (ruling 0017, shared UI tokens). `revisit_on` surfaces on its date. Isolation test extended to the new table; standalone tests for both rulings' tests of compliance. Migration SQL user-applied, delivered per standing preference. NOT authorized: hiding prospects, stage changes, automation, the branded type, tier2. Acceptance: `npx tsc --noEmit` clean; `npx next build` compiles; test counts with denominators. | 2026-09-19 |
| 48 | build | released | **Released 2026-09-21 — origin main at d396e23; migration 0067 applied by the owner and verified here by direct REST probe (both new tables answer 200), not accepted on report. Held unpushed 2 days by design: deploy-before-migration would have stopped every AI operation. DB tests executed by the owner 2026-09-21 against the live database, output counterchecked here: `scripts/test-ai-runs.ts` 52 passed 0 failed (the 9 waiting DB assertions included — terminal-written-once held against the service role, 'killed' unstorable, teardown clean).** Counterchecked in the decision space: 7 live operations instrumented (discovery_search, channel_fit, research, research_verify, strategy, draft, revisit_suggest — 10 model calls, one ledger row per user-perceived operation), birth before the first model call at every site; `scripts/test-ai-runs.ts` 43 of 43 offline including a closed-set scan that fails any future model-calling file outside the ledger; terminal-written-once enforced twice (RLS update policy gated on outcome null, plus a BEFORE UPDATE trigger that also pins birth facts and covers the service role); no storable 'killed' — a born row with no terminal outcome IS the killed run; `npx tsc --noEmit` exit 0; `npx next build` compiles. Escalations kept: three server actions throw on birth failure (pre-existing pattern, preserved); verifyRunClaims' no-material early exit makes no model call and records no run (read as compliant with clause 1); offline eval scripts call the model outside the ledger (scripts are not the live product); strategy_runs.model still writes a constant, now redundant with the ledger's captured value. Original authorization: **ruling 0026 (scope). MVP-relevant: this is how the owner watches new testers' usage.** One additive migration 0067: the run-ledger table, org-scoped per hard rule 6 (organization_id default my_organization_id(), RLS all policies org-scoped, org-match trigger for any cross-table FK, no delete policy, updates only unfinalized→terminal — follow migration 0066 as the pattern). One server-side helper owning birth (before the model call: operation, org, started_at) and finalization (model, input/output tokens from the API response, outcome completed/failed/empty). Instrument every live model-calling operation — enumerate from the code and report the list with the command that produced it; expected at least: discovery search, channel-fit, research, strategy, drafting, revisit (grep for the anthropic client's importers). Extend `scripts/test-tenant-isolation.ts` to the new table; standalone `tsx` test for the ledger's own guarantees (birth precedes call; terminal written once; killed run = born row without terminal). Migration SQL is user-applied — deliver it per standing preference. NOT authorized: billing/metering logic, credit balances, UI, changing what operations do, `lib/tier2/`. Acceptance: `npx tsc --noEmit` clean; `npx next build` compiles; all touched tests passing with counts; every instrumented call site listed in the report per ruling 0026's test of compliance. | 2026-09-19 |
| 47 | decision | approved | **Settled 2026-09-19 by ruling 0026 — a run is recorded before it runs, and how it ended is a second fact.** The load-bearing choice: the record is born BEFORE the model call, so a platform-killed run is visible as a born row with no terminal outcome — the runs the ledger most needs are the ones that never reach their own end. Consumption captured from the API response, never estimated; retained like outcomes (no delete, terminal written once); existing run tables wrapped, not rewritten. Implementation is item 48. Original: **The run ledger — nothing can be metered that is not recorded.** From decision 0006: before any billing, every AI operation is recorded with what it was, which organization ran it, when, what it consumed (model, tokens in and out), and how it ended — completed, failed, empty, or platform-killed. "A run happened" and "a run produced something" are different facts, recorded separately; the absence of a record means the run did not happen (the migration 0066 absence pattern). Decision 0004 §4 documented the gap this closes: outcomes map onto no existing column and a platform-killed run writes nothing. A new org-scoped table — hard rule 6, org-match trigger, isolation-test extension — so it **needs a ruling before code**. The ruling should also settle whether existing run tables (discovery_search_runs, research_runs) are retrofitted or wrapped. Gates: item 25's credit schedule, the failed-run policy, and (transitively) ruling 0025 clause 4's experiment. | 2026-09-19 |
| 46 | build | released | **Released 2026-09-19 — origin main at 6e6bf63, confirmed by querying the remote.** Counterchecked against the diff and a decision-space `--case maclellan` live run, not the report: the axis is read off the run's own entries (`entry.mandatory` or membership in the manifest's opportunityMatches — no second code path); every stated row carries `adv a/d` with `adv n/a` for URLs that never reached a manifest (a third value, not a forced zero); the summary partition reproduces review 0020 exactly — 13 distinct in-manifest pairs, 8 advertised, 5 unadvertised, populations named; zero-advertised manifests are announced beside their rows and in the summary, which surfaced **saddleback (0 of 32) as a second zero-signal manifest** beside mariners (0 of 60). Split selection recalls with per-population distributions print when selection is measured; exercised by typecheck only until the next paid run. `npx tsc --noEmit` exit 0. Two escalations kept: eaa all-never-retrieved again (consistent with item 33's F5 epoch note, third observation), and missio-nexus's manifest at 62 entries — opportunity promotion bypasses the 60-entry cap by design, recorded in item 32. Original authorization: **ruling 0025 (scope).** `scripts/reference-recall.ts` only: per-fact rows name each ground-truth URL's advertised status (advertised = the manifest entry is mandatory-flagged or opportunity-promoted — derived from the entries the run already built, printable in discovery-only runs); selection recall, when measured, reports the advertised and unadvertised populations separately, each with its per-case distribution (ruling 0022); the summary names a manifest's name-signal profile where it bears on the numbers (mariners: zero advertised entries of sixty). Nothing recomputed a second way, nothing hard-coded. Not authorized: `lib/tier2/select.ts`, manifest construction, item 45's plumbing. Acceptance: `npx tsc --noEmit` clean; a `--discovery-only` run prints the advertised annotation on every stated row and reproduces review 0020's 13-URL partition (8 advertised, 5 unadvertised) from the run's own data. | 2026-09-19 |
| 45 | decision | proposed | **The opportunity name is computed and then dropped before the model sees it.** From review 0020, outside its question: `scripts/reference-recall.ts` passes the opportunity name to `buildManifest` (line 120) but not to `selectPages` (line 129), whose prompt supports it — so missio-nexus's selection model was never told the subject was the Ministry Innovation Fund, and succeeded only because opportunity promotion had saturated the manifest. The one mechanism that demonstrably rescues unadvertised pages is currently a manifest-side lever invisible to the model's reasoning. Verified 2026-09-19: there is no live caller of selectPages — both callers are scripts. And the two scripts hold opposite halves of the same defect: `scripts/reference-recall.ts` gives the name to `buildManifest` (line 120) but not `selectPages` (line 141); `scripts/qualify-tier2.ts` gives it to `selectPages` (line 130) but not `buildManifest` (line 120, `{ host }` only) — a held fact delivered to exactly one of two consumers, a different one in each. Deferred by ruling 0025 clause 4: changing what the prompt is told re-prices the measurement, so this is decided with the read-then-select experiment after item 25, not before. | 2026-09-19 |
| 44 | build | released | **Released 2026-09-19 — origin main at e2b2d1f, confirmed by querying the remote.** Counterchecked in the decision space against the diffs, not the report: coverage "found" is now `read_substantive` and its doc states every coverage state is about the fetch of predicted pages, never the fact; `siteOutcome` read-and-silent maps to `not_checked` with the reason "the pages selected for X were read and do not state it; other pages were not read", and the abstention arm's reason names the model's declaration as its basis; the measurement classifies every absent judgement as declared / silent / never-covered / over-claimed with per-case rows, and refuses to score `--discovery-only` inactivity as caution. **Two decisions ratified here:** (1) a declaration contradicted by a selection scores as over-claim — not an abstention — so a future numerator can shift against review 0019's formula, annotated in the row; (2) `found_thin` keeps its name — ruling 0024's scope named only "found", and the thin state's word "found" refers to the fetch, brought to standard when next touched. Verified independently: `npx tsc --noEmit` exit 0; `scripts/test-availability.ts` 65 → 69 of 69; `scripts/test-tier2-selection.ts` 43 of 43; a live `--discovery-only` run reproduces 31/43 retrieval, 26/43 shortlist, and prints all 17 absences as never-covered. Live path untouched: `coverage: null` hard-coded at `lib/availability.ts:289`, all three live call sites verified to route through it. Original authorization: **ruling 0024 (scope).** Three files, dark pipeline only. (a) `scripts/reference-recall.ts`: absence rows and summary separate declared-unavailable, read-and-silent, and never-covered — three outcomes, never `declared || !claimed` as one. (b) `lib/availability.ts` `siteOutcome`: reasons claim what was established ("the pages selected for X", not "pages covering X"; the abstention reason names the model's declaration as its basis, not a fact about the site); coverage "found" with no extracted fact maps to `not_checked` with a reason naming what was read, per the found_thin pattern and combine's unread-source doctrine. (c) `lib/tier2/fetch.ts`: the state named "found" renamed to what it measures (loaded with substantive text; naming is build's choice, the requirement is it may not assert the fact was found); `PURPOSE_COVERAGE_STATES` and tests follow; nothing persists these strings (verified: no migration carries them, consumers are `lib/availability.ts` and ops scripts). Not authorized: prompt, tool schema, tagging, user-facing surfaces. Acceptance: `npx tsc --noEmit` clean; `scripts/test-availability.ts` and `scripts/test-tier2-selection.ts` updated and passing; ruling 0024's test of compliance answerable from every changed reason string. | 2026-09-19 |
| 43 | decision | approved | **Settled 2026-09-19 by ruling 0024 — an absence names the set it was checked over.** The trace behind the ruling: a purpose tag is a prediction; `purposeCoverage` scores a loaded tagged page as "found" with no fact checked; `siteOutcome` then asserts site-scope "checked_not_stated" ("pages covering X were read") on prediction-scope evidence — and the same collapse under-claims presences wherever selection skipped the true page, which it does on whole sites. Implementation is item 44; the selection failure itself stays item 42, classify-first. Original finding: **Absence precision on mixed sites is 0 of 7 — the honesty number fails exactly where honesty is hard.** From review 0019: of 17 human-recorded absences, the 6 the system respected all sit on the two sites that publish nothing at all; on every mixed site — a funder publishing some facts and not others — the selector claimed a page for the unpublished purpose (ncf 0/2, missio-nexus 0/1, mariners 0/2, signatry 0/2). The 35% mean averages two populations per ruling 0022 and describes neither. This is a selection-layer proxy (a claimed page, not yet a claimed fact to a user), which bounds the pipeline's caution at its cheapest stage. It touches the product's core claim — the brief names over-claimed absence as how a user ends up trusting a judgement resting on nothing. Needs a ruling on what the selector must do with an unavailable purpose before any fix. `unavailablePurposes` exists in `lib/tier2/select.ts`; **whether it was ever used on this run is not derivable from the output** — the check scores `declared || !claimed` as one outcome, so a declared absence and a silent non-claim are indistinguishable in what was printed. (Corrected 2026-09-19: this row briefly asserted "used zero times", a value the evidence does not carry — the defect shape this ledger exists for, written by the decision space while recording a review about it.) Separating those two outcomes in the output is a natural companion to item 39's distribution lines, but is unauthorized until ruled. | 2026-09-19 |
| 42 | decision | approved | **Settled 2026-09-19 by ruling 0025 — the ceiling is recorded and priced, not tuned away; lifting it waits for item 25.** Owner's decision, recorded in the ruling: honesty now, capability when its price is known. Work authorized is item 46 (the measurement split). Selection mechanics frozen; the read-then-select experiment rides behind item 25 with item 45. Diagnosis record: **Diagnosed 2026-09-19, counterchecked — the failing class is a property of pages, not sites: a true page whose manifest line does not advertise its purpose.** `docs/reviews/0020-selection-misdirection-diagnosis.md`, deterministic reconstruction, no model call. On the recorded run the advertised/unadvertised partition separates perfectly: 8 of 8 advertised true URLs selected, 0 of 5 unadvertised (populations: the 13 distinct in-manifest true URLs across 7 sites; "advertised" = mandatory-flagged by `isMandatoryPath` or opportunity-promoted — both deterministic). The boundary runs through antioch itself: its one miss is its homepage, titled "Skip to Content". The three failing sites are the set's two DAF sponsors and its church — every true page name-silent — so accepting this ceiling means structural blindness where funders are not conventional grantmakers. Group collapse contradicted, cap excluded, position subordinate (mariners missed at index 6/60), noise not needed but unpriceable without a paid re-run. Ruling wanted, two parts: (a) bind the measurement to report selection recall split by the advertised axis; (b) direction on unadvertised pages — a cheap read-then-select pass vs recording the bound as a known ceiling; per review 0009 vocabulary additions are not a fix. Original finding: **Selection fails all-or-nothing by site, and misdirects rather than abstains.** From review 0019: of 7 cases whose pages reached the shortlist, 3 selected everything or nearly (maclellan 4/4 available, stewardship 5/5, missio-nexus 4/4; antioch 4/5), and 3 selected NONE of the true pages (ncf, mariners, signatry) — while on those same sites selecting other pages tagged for purposes the human recorded as unpublished. So the failure is not caution: wrong pages for absent facts were chosen over right pages for present facts. Ruling 0022's shape at the selection stage; same discipline as item 33 — classify the failing site shape before touching `lib/tier2/select.ts`, no per-case fix (review 0009). Candidate diagnosis to run cheaply: whether the mis-selected pages share a purpose-tagging pattern. Single run; variance unpriced. | 2026-09-19 |
| 41 | decision | proposed | **A truncated row is invisible to ruling 0023's classification entirely.** Escalated by build during item 40, correctly left unfixed: a `|`-line with fewer than seven cells returns null from `tableRowCells` (`scripts/ledger-check.ts`), so it is neither counted in the row population nor classified as header, separator or data — an open-items row missing a column vanishes from both the ruling-0021 count and the ruling-0023 violation path. Same shape as item 31, one level down: membership is currently decided by cell count before structure is ever examined. Ruling 0023's clause binds this enumeration when next touched; deciding whether a short `|`-line inside the open-items block is a violation (probably: any line starting with `|` that is not header, separator, or a well-formed data row) needs a ruling amendment or a successor before code moves. | 2026-09-19 |
| 40 | build | released | **Released 2026-09-19 — origin main at 4eba7f7 carries cd88f31, confirmed by querying the remote.** Verified in the decision space, not accepted: `scripts/test-ledger-check.ts` 76 → 82 of 82 passing; `npx tsc --noEmit` exit 0; `--quiet` output byte-identical before/after (diff on stashed captures); and the test-of-compliance row demonstrated directly — a `4a` data row produces one violation naming the row verbatim while the well-formed row beside it still parses. Header identified by its literal id cell, separator by dash/colon/whitespace cells, alignment-colon separators included. Original authorization: **ruling 0023 (scope).** `parseOpenItems` (`scripts/ledger-check.ts`) classifies table rows structurally — header by its column names, separator by dashes/colons/whitespace, everything else a data row — and a data row whose id cell fails `^\d+$` is a reported violation naming the row, never a silent skip. No well-formed row's parse changes. One failing example per new failure mode in `scripts/test-ledger-check.ts`, per its existing pattern; the ruling-0021 summary line keeps printing both counts. Acceptance: ruling 0023's test-of-compliance row makes the check fail naming the row; the full suite passes; `npx tsc --noEmit` clean. | 2026-09-19 |
| 39 | build | released | **Released 2026-09-19 — origin main at 4eba7f7 carries d72cca7, confirmed by querying the remote.** Verified in the decision space, not accepted: the tally sits beside the existing accumulators consuming the same t/d/s/r values the per-fact rows print; a real `--discovery-only` crawl reconciles exactly (retrieval: 6 complete, 2 partial, 2 zero, of 10 contributing, summing 31/43; shortlist: 5 complete, 2 partial, 3 zero, summing 26/43); an independent second run reproduced the identical lines; `npx tsc --noEmit` clean. The 6/2/2 differs from item 33's 8/0/2 because the tool ranges over all 43 stated judgements, dead URLs included — the two 404s from item 32 are what turn maclellan and ncf partial. Both numbers are right about their own populations; the tension is item 32's question, now visible in the tool's own output. Original authorization: **ruling 0022 (scope).** One change in `scripts/reference-recall.ts`: the summary block prints, beside each recall it reports, the per-case distribution over the cases contributing to it — complete / partial / zero — so a bimodal result is visible in the run's own output and the first case to land *between* the modes is announced by the run that observes it. The distribution is computed per recall, because they differ: cma is complete at retrieval and zero at shortlist. Cases contributing no `stated` judgements stay excluded and the printed line names the contributing-case count (ruling 0021). Output only — no change to discovery, selection, fetch, the cap, or `docs/reference-set/cases.json`. Acceptance: a `--discovery-only` run prints a distribution line for retrieval and shortlist; `npx tsc --noEmit` clean; the printed distribution reproduces from the per-fact rows above it. | 2026-09-19 |
| 38 | build | proposed | **Fourth instance today: a hard-coded count in an interface string that drifted from what it counts.** `scripts/test-tenant-isolation.ts:322` prints "Seeded one row per table (seven total) under Org A." Measured 2026-09-18 over lines 230-322, the range between setup and the first assertion: **nine** distinct tables are touched, not seven — `prospects` plus the eight research tables. The two migration 0066 tables are seeded later, at 464 and 471, so they are outside the sentence's scope and are not the discrepancy. Cosmetic, in a test harness, and worth recording anyway because it is the same shape as item 24's 407 citations, item 33's bimodal mean, and item 23's "10 new assertions" that were 8: **a number written once and never re-derived from the thing it describes.** Ruling 0021 already binds reports; this is the same failure inside a program's own output. Fix is to count the seeded tables rather than state them. | 2026-09-18 |
| 37 | decision | approved | **Resolved 2026-09-18 — 0066 was genuinely not applied, and is now.** `select count(*) from information_schema.tables` returned 0, then 2 after re-running. The wrong-project theory was wrong: the dashboard breadcrumb read `Oranjoz / fundraising-ai / main PRODUCTION` against ref mbsxeyoxojcqetrmwgba, matching the app. Root cause not established beyond that the first attempt did not reach this database — five `Untitled query` tabs were open, which is the most plausible route. **The lesson worth keeping is the diagnostic one:** Supabase prints "Success. No rows returned" both for a SELECT that found nothing and for a successful CREATE TABLE. One string, two opposite meanings — the project's own governing defect, arriving in the tool used to diagnose it. A probe that cannot return an empty result (`count(*)`, which always yields one row) distinguishes them. Originally: 0066 not visible though reported applied. | 2026-09-18 | Found 2026-09-18 while running the repaired isolation harness. Three probes, none of them an inference from a name: an insert returns `PGRST205 Could not find the table 'public.prospect_outcomes' in the schema cache`; a direct select on both new tables fails while `qualification_stages` from migration 0064 succeeds, so the client and credentials are fine; and the REST root exposes 29 table definitions with nothing matching outcome — 29 being exactly the pre-0066 count already recorded in item 28. **What this cannot distinguish** is "never applied" from "applied, PostgREST schema cache never reloaded", because `.env.local` carries no `DATABASE_URL`, so no `information_schema` read and no cache reload were possible. Probe 3 makes staleness unlikely rather than impossible. Two consequences: item 23 cannot reach `tested`, since its 8 ruling-0019 assertions are exactly the ones that cannot execute; and whatever the cause, the ledger must not record 0066 as applied on the strength of a report. Resolve by re-running the SQL — it is idempotent in effect only if the tables are genuinely absent, so check first. | 2026-09-18 |
| 36 | build | released | **Live at fd53a96, 2026-09-18.** Fixed and proved by two consecutive runs with no manual SQL between — run 2 opening with "nothing to remove" is the evidence run 1 left nothing.** 17 passed, 0 failed over the assertions that execute; 0 cleanup problems across both runs. Setup moved inside the `try`; teardown now purges by identity rather than by captured ids, so it covers a setup that failed before returning any. Counterchecked in the decision space, and this one mattered because it deletes from the live database: **no `like` or `ilike` anywhere in the file** — two email literals and two org-name literals, matched by equality and re-filtered in TypeScript — plus a guard that refuses to purge any matched org holding a profile outside those two emails. Real data intact per build's post-run read: 2 organizations (Village Worship Initiative, Tunde Aviation), 2 real profiles, 32 prospects unchanged, 0 test rows. Review: `docs/reviews/0017-the-isolation-harness-stops-leaking.md`. Original defect: **the harness leaked its own setup and then blocked itself.** Found 2026-09-18 running it after migration 0066: it failed at `scripts/test-tenant-isolation.ts:60` because a user from an earlier run was still registered, so no assertion executed and **nothing about isolation has been established either way**. Cause read from the source, not inferred: `main()` creates both throwaway orgs and users *before* entering the `try`, while the cleanup lives in that block's `finally`. Any failure during setup — including the second create — leaves the first user, its profile and its organization behind, and every later run then fails on the duplicate email. Its own header says it "tears everything down in a finally block — safe to re-run", and four lines later explains how to clean up by hand when that is untrue; both halves were written, only the second is accurate. Fix shape: the cleanup must cover the setup phase, and setup should be idempotent — delete any pre-existing test org and user before creating, so a leaked run heals instead of blocking. Worth noting against this project's standing preference for permanent test tools over throwaway scaffolding: a permanent tool that needs manual SQL surgery before each re-run is not yet permanent. Blocks item 23 reaching `tested`, since this is the only check on hard rule 6 and `CLAUDE.md` states nothing else catches it. | 2026-09-18 |
| 34 | decision | approved | **Settled 2026-09-19 by ruling 0027 — a recorded outcome can be retracted, and a retraction is an appended fact.** Both principles survive: 0019's retention (nothing deleted or edited, ever) and the standing view that an unrevisable confirmation is a trap. Effective outcome is derived in one place; after retraction the prospect stands as if nothing were recorded. Implementation is item 49. Original: **A decline recorded by mistake has no way back.** Raised by build rather than decided, correctly. Migration 0066 grants no delete policy on prospect_outcomes by design — ruling 0019 is explicit that this is retention, not deletion — and "reversible" in that ruling attaches to the `never` disposition, not to the outcome itself. So a misclick that records a funder as having declined is permanent. This codebase has twice held that a confirmation a person cannot revise is a trap, which is the argument for a retraction path; ruling 0019's retention principle is the argument against deleting the row. Those are compatible: a retraction is an appended fact, the same shape the disposition log already uses. Needs a ruling before code, because "what may be undone" is direction. | 2026-09-18 |
| 35 | decision | approved | **Settled 2026-09-19 by ruling 0028 — a surface that offers work consults the outcome in effect; closed is shown, not worked.** Closed prospects leave work lists but never disappear (a no is data); revisit_on is scheduled work; every surface derives from ruling 0027's one derivation. Item 20's tier2 half stays deferred — this covers outcomes only, which face testers Monday. Implementation is item 49. Original: **A permanently-closed funder can still appear under "Due now", and the Pipeline board shows no outcome at all.** Both existing surfaces filter on `next_action_due` and know nothing about outcomes, so a prospect closed with `never` keeps surfacing as work. Build found this and did not fix it, correctly — changing what those views show is a behaviour change ruling 0019 does not authorize, and it is the same shape as item 20 (two systems with something to say about one prospect, and no stated display rule). Probably wants deciding alongside item 20 rather than separately. | 2026-09-18 |
| 33 | decision | approved | **Settled 2026-09-19 by ruling 0022 — a rate aggregated over cases carries its per-case distribution, and a rate no case resembles is not evidence of behaviour.** The 72%/76% may not be quoted alone again; the record of finding is "8 of 10 contributing cases complete, 2 of 10 zero, none partial" (retrieval, over live URLs). Work authorized under its scope is item 39 only. F4 (pma), F5 (eaa) and F6 (cma's cap loss) stay open in this row: the next move is classification — establish F5's cause, and establish how common F4's cross-subdomain shape is among real funders — then decide what retrieval must cover. F5 sharpened 2026-09-19 by item 39's verification runs: the comment at `scripts/reference-recall.ts:121` records an epoch in which eaa's pages were RETRIEVED and dropped by reduction, while both measured runs (2026-09-18 and 2026-09-19) show them NEVER RETRIEVED — so eaa's failure has apparently moved between stages over time, which is item 32's live-network decay arriving inside F5 and strengthens classification-before-tuning. Tuning aimed at the mean is unauthorized work. Original finding: **The 72% is bimodal, and the mean hides the only thing worth knowing.** Counterchecked in the decision space from review 0015's per-case table: of the 10 cases contributing judgements, **8 retrieve 100% of their live ground-truth URLs and 2 retrieve 0%. None is in between.** Population: the 41 `stated` judgements whose URL returned 200 on 2026-09-18, i.e. the 43 less the two dead ones in item 32; over that set retrieval is 31/41 (76%) and shortlist 26/41 (63%). So retrieval is not mediocre-across-the-board and does not want general tuning — it is reliable, and it misses entire sites of a particular shape. The two failures have named, different causes in review 0015: `pma` retrieves 24,269 URLs from exactly one host while all 5 of its ground-truth URLs live on `centernet.pcusa.org` (cross-subdomain, F4); `eaa` follows all sitemap children untruncated and still never surfaces either page (F5, cause not established). A third case, `cma`, retrieves all 5 and loses all 5 to the 60-entry cap, which is the entire retrieval→shortlist gap (F6). **This is ruling 0004's failure shape — one number averaging two different facts — surviving in a report that otherwise complies with ruling 0021.** Naming a denominator does not make a mean meaningful. Needs a ruling before any code moves; per review 0009 a per-case fix is not a fix. Minor: F7, absence precision is computed under `--discovery-only` and would be vacuously 17/17, suppressed at `scripts/reference-recall.ts:149` but computed at `:126`. | 2026-09-18 |
| 32 | decision | proposed | **The frozen reference set has decayed, and `frozen: true` cannot stop that because the population is a live network.** Three findings from review 0015, all verified in the decision space rather than accepted. (a) **Two of the 43 ground-truth URLs are 404** — the maclellan application_process fact and the ncf recipient_restrictions fact — re-checked independently with `curl -L`, both 404. Discovery cannot fetch a page that does not exist, so both count as retrieval misses against a system that did nothing wrong. (b) **The set no longer contains a blocked-site case**: `ronald-blue` is recorded as `failureClass: "Site blocked or unreachable"` and today returns 200 via redirect with 751 URLs retrieved. Whatever it guarded is unguarded, and `saddleback` and `ronald-blue` both record zero `stated` facts, so neither moves either recall. (c) **The measurement is not reproducible over time**: `eaa` is recorded at 5,566 URLs and returned 10,664 today. The decision is not "fix the fixture" — it is what `frozen` means when the thing frozen is a pointer to someone else's website, and whether a recall number carries the date its population was observed. Build correctly refused to edit a frozen fixture and reported 31/43 as the result with 31/41 as secondary. Addendum 2026-09-19 (item 46's run): missio-nexus's manifest measured 60 entries in reviews 0015/0020 and 62 today — opportunity promotion bypasses the 60-entry cap by design (`lib/tier2/manifest.ts`), so manifest size itself moves with the live site; a fourth instance of the population moving under a frozen pointer. | 2026-09-18 |
| 30 | build | tested | **Measured 2026-09-19 — run by the owner with the API key, full output supplied and counterchecked in the decision space from the per-fact rows, not accepted from the summary.** Selection recall 17/43 (40%) — per case 2 complete, 2 partial, 6 zero of 10 contributing; conditional on the shortlist, 3 of 7 cases with available pages selected NONE of them (ncf 0/2, mariners 0/3, signatry 0/3). Fetch recall 17/43 (40%) — but 17 of 17 selected pages read, so fetch lost nothing and the 40% is selection's ceiling passed through. **Absence precision 6/17 (35%) — and 0 of 7 on the four mixed sites; all 6 correct absences sit on the two publish-nothing sites (ronald-blue 4/5, saddleback 2/5).** Retrieval and shortlist identical to both discovery-only runs, third consecutive reproduction. Evidence: `docs/reviews/0019-selection-and-absence-first-measurement.md`. Single paid run; selection variance unpriced. Consequences split to items 42 and 43. Original: **Blocked half of item 4 — selection and fetch recall.** Split from item 4 on 2026-09-18. Needs a model call and therefore an API key the assistant sandbox does not have: `selectPages` (`lib/tier2/select.ts`) and `fetchSelectedPages` (`lib/tier2/fetch.ts`) both run only when `--discovery-only` is absent (`scripts/reference-recall.ts:100` and `:105`). Run as `npx tsx --env-file=.env.local scripts/reference-recall.ts`. Reports selection recall, fetch recall and absence precision — the last of these has no discovery-only equivalent, so over-claiming an absence is currently unmeasured. Blocked on the owner running it, not on a ruling. | 2026-09-18 |
| 31 | decision | approved | **Settled 2026-09-19 by ruling 0023 — a row that does not parse is a violation, not a skip; membership is decided structurally, and item ids are settled as numeric.** Implementation is item 40. Original finding: **A malformed item id is silently skipped rather than failing.** `parseOpenItems` (`scripts/ledger-check.ts:532`) drops any row whose id is not `^\d+$`, with the comment "header and separator rows". That is correct for the two rows it was written for and wrong for everything else: an item numbered `4a`, `4-a` or `29 ` parses as a table row, fails the id test, and vanishes from every ruling-0015 check — status, owner, evidence, `released`-against-branch. It is not reported as skipped. Found 2026-09-18 by the decision space renaming item 4 to `4a` and watching the open-item count fall from 15 to 14 with the check still reporting `protocol intact`. **This is the same shape as ruling 0021's finding one turn earlier: a count taken over a population that silently excluded something.** Candidate invariant: a row inside the open-items table that does not parse as an item is a failure, not a skip — the check distinguishes "not an item row" (header, separator) from "malformed item row" structurally rather than by whether the id happens to be numeric. Needs a ruling before the parser changes. | 2026-09-18 |
| 29 | build | released | **Released 2026-09-18 — on the origin main branch at 79161f5, confirmed by querying the remote rather than local state.** Two commits: de6fd05 (ruling 0021, decision 0005, the measurement) and 79161f5 (the tooling). Nothing user-facing deployed — the diff is `docs/` and `scripts/` only, no `app/`, no `lib/`, no migration. Built and counterchecked before landing. Built in an isolated worktree off `main` at 647c8fd, then brought across after review. Re-verified after landing: `scripts/test-ledger-check.ts` 76 of 76 passing in the main tree, `npx tsc --noEmit` exit 0, check reports `protocol intact`. It demonstrated itself on its own landing: the citation count moved 375 → 403 and the printed population moved with it, 56 → 57 `.md` files, because review 0014 was added — under the old output that was a 28-citation jump with no way to tell whether the corpus grew or the check changed. Evidence, verified in the decision space rather than accepted: the candidate script run against *this* tree produces counts identical to the current one (21 rulings, 19 open items, 373 citations, 0 migrations), same two warnings, same verdict, same exit 0 — so it is output-only in fact, not just in intent. All four printed populations reproduce from the printed commands alone. `--quiet` output is **byte-identical** (301 bytes), so the Stop hook this session runs every turn is unaffected. `scripts/test-ledger-check.ts` 61 → 76 passing in the worktree. Review: `docs/reviews/0014-a-count-names-its-population.md`. **Decided here: the summary is a block, not one physical line.** Build read the scope's word "line" as descriptive and escalated rather than deciding silently, which is the correct move; it is also correct on the merits, because four populations and four commands on one physical line would satisfy the wording and defeat ruling 0021's own test of compliance — that a reader can reproduce the population. Not a ruling: this is how one job renders output, and ruling 0001 draws the line at invariants. **Settled by ruling 0021 — a count names the set it ranged over.** Implementation is one change in `scripts/ledger-check.ts`: its summary line names the population beside each count it prints, so the next divergence shows in the output instead of surfacing by accident. Authorized now; handoff above. Original finding: **a reported number did not name the population it was measured over — third occurrence.** Item 24's evidence row said 407 citations; the true figure is 337, the gap being 17 duplicate files the run happened to include. Before it: "78% coverage" that was not coverage, and "47% discovery recall" that conflated never-retrieved with retrieved-then-reduced. The ledger already has habits against *mislabelled* numbers (`ROLE-build.md`, "name what you measured") and against *collapsed* numbers (same file, "never let two facts collapse"). Neither catches this one, because 407 was correctly labelled and measured a single thing — it just ran over a file set nobody stated. **Candidate invariant: a count reported as evidence names its denominator — the set counted and how that set was determined — or it is not evidence.** That is general, testable, and not case-specific, so it is a ruling rather than a decision. Worth settling *before* item 4's recall measurement, which is the next number this project will be asked to trust and is currently blocked precisely on producing one. | 2026-09-17 |
| 2 | build | released | **Step 4 — 0009 wired.** `deriveAvailability` now reaches the product through one mapping function, `availabilityForResearchRun` (`lib/availability.ts:274`), called from `lib/prospect-intelligence.ts:417`, `research-actions.ts:251` and `app/admin/research/page.tsx:273`; `obtainableGaps` reaches every gap surface through one filter, `offerableGaps` (`lib/availability.ts:337`). All six acceptance checks met — check (2) only partly, see the review: `not_applicable` is unreachable in the live path because neither route to it has a recorded input. Evidence: `docs/reviews/0012-step-4-wiring-availability.md`; `scripts/test-availability.ts` 37 → 65 passing; 660 assertions passing across 13 test files, 2 skipped for missing Supabase env; `npx tsc --noEmit` clean; `npx next build` compiles, 28 routes. **Released 2026-09-17** — corrected from "not released, not merged", which item 27 had already falsified. Verified in the decision space against the artifact, not the row: `git merge-base --is-ancestor build-1-qualification main` succeeds, and `lib/availability.ts`, `lib/discovery-handoff.ts`, `lib/legitimacy.ts`, `lib/qualification.ts` and migrations 0063–0065 are all present on `main`. Ruling 0010's grep test re-run here: 14 call sites across 5 production files. | 2026-09-16 |
| 26 | decision | approved | **Settled 2026-09-21 by ruling 0030 — the tab serves both decisions and carries a ledger for each.** A second consumer gets a second call under ruling 0011; neither keys set widens. Implementation is item 53. Original: **Which decision does the Research tab serve?** The ledger there names `screening` as its consumer (ruling 0011 forbids a default, so a call had to be made; the reasoning is in review 0012). But the dossier that tab shows is also the input to a strategy run, and strategy's required set has 8 keys screening's does not — `accepts_unsolicited`, `deadline`, `invitation_mechanism`, `fiscal_sponsorship_rules`, `grant_size_range`, `median_grant_size`, `international_reach`, `total_assets`. Under ruling 0017 those 8 are not displayed with a reason on that screen. If the tab serves both decisions, the fix is a second ledger beside the first, not a change to either function. Build did not decide this — which decision a screen serves is direction. | 2026-09-17 |
| 17 | build | released | **Rulings 0008 and 0011 complete.** `deriveAvailability` evaluates every source a fact could come from and combines them (unread outranks failed outranks silent); `keys` required, no default. Evidence: `scripts/test-availability.ts` 29 → 37 passing, including 0008's compliance case and a self-contradiction guard; tsc clean. Released with item 2 on 2026-09-17 — corrected 2026-09-17 from "not released", which the item 27 merge had already falsified. | 2026-09-16 |
| 16 | decision | approved | **Tracking, not work.** The site half of the treadmill stays open by construction until Tier 2 reaches the live path — required by ruling 0013 so a partial close cannot read as a complete one. Under ruling 0016 the lift condition is now concrete rather than pending a cutover: it closes when a ruling lands `lib/tier2/` in the live path, same pattern as 0003 and 0009. Nothing to supersede. Keep open for exactly as long as `coverage` is null in the live run. | 2026-09-16 |
| 23 | build | released | **Live on the origin main branch at fd53a96, 2026-09-18 — confirmed by querying the remote, not local state. First user-visible behaviour to ship from this session.** Verified against the live database before merging: **26 passed, 0 failed** over every assertion in `scripts/test-tenant-isolation.ts`, with migration 0066 applied and confirmed by `information_schema` returning 2. All 8 ruling-0019 assertions executed for the first time and all passed — including the three that are not about cross-tenant leakage at all: Org A cannot UPDATE its own disposition, cannot DELETE its own disposition, and cannot DELETE its own outcome. **Retention is therefore enforced by the absence of an RLS policy, not by the interface declining to offer a button.** Teardown verified clean. Not `released`: uncommitted and unmerged, and this one touches `app/`, `lib/`, `components/` and a migration, so merging deploys user-facing behaviour. Originally: built 2026-09-18. Two tables: the outcome, and an append-only disposition log in which **`undecided` is the absence of a row**, so nothing stores it and nothing can get it wrong. Counterchecked in the decision space against the artifacts: 0066 is additive under ruling 0020 — two new tables, every `alter table` targeting those same new tables, no existing table touched, no `add column`; all 5 RLS policies scoped by `my_organization_id()`, none `using (true)`, plus two org-match triggers closing the FK-bypasses-RLS hole that `docs/decisions/0001-multi-tenancy.md` leaves open elsewhere; of 16 `never` literals in app, lib, components and supabase exactly one assigns the value (`lib/prospect-outcomes.ts:73`, guarded), and no `default 'never'` exists in any of 66 migrations. **The branded-type guarantee was re-proved here, not accepted**: a hand-written choice fails with TS2322, so "never comes only from a human" is enforced by tsc rather than by wording. Evidence: `scripts/test-prospect-outcomes.ts` 121 passed 0 failed; `npx tsc --noEmit` exit 0; `npx next build` compiles, 28 routes. **Unverified until the SQL is applied**: no disposition has ever been stored, and `scripts/test-tenant-isolation.ts`'s **8** new assertions were deliberately not run — corrected 2026-09-18 from "10", which came from a build report and was never checked; `check(` occurrences go 19 → 27 between HEAD and the tree because they write to the live database. Review: `docs/reviews/0016-a-declined-prospect-keeps-its-reason.md`. Ruling 0019: record a prospect outcome when a funder declines — reason plus a three-valued revisit disposition (`revisit_on <date>` / `never` / `undecided`). `never` only ever from an explicit human action; absent means `undecided`, never `never`. Reversible, retained, not deletion. Excludes referral capture and any terminal stage. Needs a migration (user-applied, SQL inline in the closing message). | 2026-09-17 |
| 25 | decision | approved | **Re-scoped 2026-09-19 by decision 0006 — credits are the unit of all AI usage, priced per operation type, never per token.** Owner's decision; supersedes decision 0004. (b) is dissolved rather than answered: "search" is no longer a billing word, each operation carries its own credit price. (c) becomes the failed-run policy — recorded recommendation: a failed, empty or platform-killed run costs nothing — which is only possible on top of item 47's run ledger. What remains here: set the credit schedule from measured per-operation costs once the run ledger records them (ruling 0021 applies to a price's basis), then price the ruling-0025-clause-4 experiment. Original: **Commercial model recorded as provisional** — subscription with a set number of searches, then credits. See `docs/decisions/0004-commercial-model.md`. Three things followed. **(a) is closed — verified in the decision space 2026-09-17**, not accepted from item 27's report: the only surviving "credits" token in `app/` and `lib/` is a comment at `research-panel.tsx:181` recording the removal, and the live string at `:187` reads "it costs real money and takes several minutes", which is true under any pricing model. Remaining: (b) "a search" currently means two unrelated operations (a channel-wide discovery search, and a single-prospect research run) — settle before pricing; (c) a failed, empty or platform-killed run does not map to any existing column — settle before metering. **Nothing blocks work in flight; all three get expensive once customers are on a plan.** | 2026-09-17 |
| 21 | decision | approved | **Settled 2026-09-21 by ruling 0029 — one send path, born behind one human click.** Five clauses: one funder-facing send module with one importer (closed-set scan), a handler that re-verifies approved/org/never-sent server-side at send time and sends only on a confirmation showing exact content, one-draft-one-send enforced in the database, provider response captured never retyped (attempted-unconfirmed is a recorded state), and no batch/schedule/trigger/retry ever. Implementation is item 52 (Slice 8). Original: **From the doc audit — remaining half.** Hard rule 1 ("no auto-send") is currently true only *by absence*: there is no send code at all, so nothing enforces it. The moment Slice 8 starts, that guarantee has to move into construction — a send path reachable only from a handler taking an approved draft id and a live human session. Not urgent; decide before Slice 8 is picked up. The other half of this item became ruling 0019. | 2026-09-17 |
| 20 | decision | proposed | **Deferred by ruling 0016, not dropped.** When the live Slice 3 rules engine and the qualification pipeline both have something to say about one prospect, what does the user see? Not a precedence question — neither system acts, both are advisory under hard rule 3 and ruling 0002 — so it is a display decision. Revisit when `lib/qualification.ts` approaches landing; designing it now would design against output that is still changing (same reasoning as ruling 0007). | 2026-09-17 |
| 24 | build | released | **Ledger tooling — done and in the main tree.** Rulings 0012, 0014, 0015, 0018 and 0020 implemented in `scripts/ledger-check.ts` (931 lines, checks split into exported functions with git behind a port so they can be driven against fixtures). Evidence: `scripts/test-ledger-check.ts` 61/61 passing with one failing example per rule; the live check run against this repo found 4 real stale citations in `docs/decisions/` which are now fixed; **337** citations verified per run; `--seal` leaves `.baseline.json` and `.confirmed.json` byte-identical. Counterchecked in the decision space, not accepted on report. **Evidence row corrected 2026-09-17: this said 407.** The report was measured against a tree polluted by 44 duplicate `* 2.*` files left by the project's move from `~/Documents` to `~/Developer`, 17 of them under `docs/`. Cause established by experiment, not inference — re-adding two duplicates moved the count 337 → 376, so 17 account for the gap. The check was correct throughout; only the recorded number was wrong. The duplicates are deleted and the tree is clean, so 337 is reproducible. **This is the third instance of the failure the protocol is built against — a number measured over the wrong population — after "78% coverage" and "47% discovery recall". The first two were caught by reading the underlying words; this one was caught only because the tree changed underneath it.** See item 29. | 2026-09-17 |
| 28 | decision | proposed | **Now has a live example, found 2026-09-18.** `lib/prospect-outcomes.ts:28` explains the branded type by naming the function it protects — and that function does not exist. The real chain is setRevisitDisposition → parseRevisitChoice → writeRevisitDisposition; the name in the comment was never written. The mechanism is sound and was independently re-proved, so nothing is broken; but it is a claim citing an artifact that is not there, sitting four lines above a comment arguing that wording is never the last line of defence. **Ruling 0018's symbol check is exactly what would have caught it, and it shipped disabled pending this item.** The cost of leaving this open is no longer hypothetical. Original: **Ruling 0018's mechanical half shipped for paths only; table names and symbols need a ruling, not a build decision.** Table names are checkable with near-zero noise — 29 tables parsed from the migrations, exactly **one** cited name in `docs/**` has no table behind it. But that one sits inside ruling 0018 itself, in the table of wrong claims the ruling exists to record, in a file that is immutable. Enabling the check makes the ledger permanently red with no legal remedy. Symbols give 19 misses, most legitimate — `revisit_on` in the Slice 7 doc is unbuilt, not wrong. Both need a vocabulary for *quoted, not asserted* and *planned, not present*. Build stopped rather than guessing, correctly. The mechanism is probably already discovered: build wrote the six bad paths in its own report **without backticks** so the report would pass its own check — so backticks mean "this is real", plain text means "I am discussing it". The remedy for the one immutable violation is an entry in `.baseline.json`'s new `exempted` schema, dated and reasoned. | 2026-09-17 |
| 1 | decision | approved | **0002, 0003 and 0006 are now recorded as `confirmed` in `rulings/.confirmed.json`**, each with the file and line it was checked against — the mechanism ruling 0012 called for, built under item 24. The check went from five warnings to two, and the two that remain are the ones genuinely unverified. **0005 and 0007 stay `unconfirmed`**: process decisions with no code artifact, and `search_session_transcripts` is unavailable in unsupervised mode. Retry with tool approval. | 2026-09-16 |
| 5 | decision | proposed | Untested end to end: whether newly discovered named opportunities carry name + source URL through the handoff contract in a live run. 0003 is confirmed as code; confirming code exists is not confirming it runs. Needs one live run, same as item 4. | 2026-09-16 |
| 4 | build | tested | **Measured 2026-09-18. Retrieval recall 31/43 (72%), shortlist recall 26/43 (60%)** — never blended; unit is one (fact key, ground-truth URL) pair, population is the 43 `stated` judgements in `docs/reference-set/cases.json`, reproduced independently in the decision space. All 12 sites answered; only 10 contribute judgements. Evidence: `docs/reviews/0015-discovery-recall-measurement.md`; run twice, byte-identical. Selection, fetch and absence precision remain unmeasured — item 30. **Originally: the real bottleneck, open all session.** Step 3b: selection and fetch recall against the frozen reference set has never been measured. Build 1 is 4 steps into 10, and steps 5–10 all sit on top of retrieval whose recall is unknown. With two orgs there is no usage data either, so the reference set is the *only* evidence available about whether any of this works. **Re-scoped 2026-09-18 by decision, against the script rather than the item text — the item was more blocked than the code is.** Two corrections. (a) The reference set is **not** a blocker: `docs/reference-set/cases.json` is fully populated — 60 of 60 cells across 12 cases carry a recorded judgement, 43 `stated` and 17 `not_stated`, `frozen: true`, none `unreviewed`. (b) "Needs a user-run model call" is true only of the **selection and fetch** half. `scripts/reference-recall.ts:61` defines `--discovery-only`, added for exactly this situation, and its own comment states discovery is deterministic, needs no model, and *is the ceiling on every stage after it*. So retrieval recall and shortlist recall are measurable now with no API key, and they bound whatever the paid half would report. Splitting the item accordingly: **this item (4) is now the discovery half only**, and the blocked selection+fetch half is **item 30**. Ids stay numeric — see item 31: `parseOpenItems` (`scripts/ledger-check.ts:532`) silently skips any id that is not `^\d+$`, so a row numbered `4a` is not validated at all. Per ruling 0021 any number either half produces names its denominator — the script already does this by construction, reporting three recalls separately and refusing to blend them. | 2026-09-16 |
| 18 | build | released | **Migrations 0063–0065 are applied to the live database** and verified additive (ruling 0020 clause 3): every new column is `not null default <value>`, every new check bounds only a new column, `qualification_stages` is a new table. Closes when the branch merges and schema and code are back in step. | 2026-09-17 |

## Closed items

| id | subject | closed |
|----|---------|--------|
| 8 | **Build 1's landing. Settled by ruling 0016**: no cutover event — each module lands when a ruling establishes it is correct, gated by ruling 0010. Ratifies what was already happening unnamed: `discovery-handoff` landed under 0003, `availability` is landing under 0009. The item's own premise was wrong — "no defined landing" described an *undeclared* one already used twice. The display question survives as item 20, deferred. | 2026-09-17 |
| 9 | **Roadmap docs refreshed 2026-09-17.** Every slice doc now carries an "As built" section stating what actually shipped; `architecture/OVERVIEW.md` rewritten against the real 65 migrations, lib and app layout, and all three invariants re-verified as enforced; `slices/README.md` now carries per-slice status and explains Build 1's relationship to the slices; `decisions/0002`'s "ships dark / if ever" statements corrected against ruling 0016. Findings that needed a decision rather than a doc edit became item 21. | 2026-09-17 |
| 6 | **Branch merge. Decided 2026-09-17: `build-1-qualification` merges to `main` as the last act of Step 4**, not as a follow-up — ruling 0020 clause 2, a landing merges in the work that performs it. The earlier "do not merge yet" was decided on a false picture: it assumed parts of Build 1 were already live, and ruling 0020 established that none are. Verified safe — migrations already applied and additive, no non-additive change in the 7 commits. Conditional on the acceptance checks passing; a failing build does not merge. | 2026-09-17 |
| 27 | **Step 4 merged and deployed 2026-09-17.** Counterchecked in the decision space before the merge, not accepted on report: `deriveAvailability` went from zero production callers to fifteen across five files; three false-completeness strings deleted; the "spends credits" claim replaced with what is true regardless of pricing; 65 availability assertions passing; `next build` clean. Merged fast-forward, pushed by the owner, confirmed live on GitHub by querying the server rather than local state. The three narrowings build flagged rather than rounded up are carried as items 16, 26 and 28. | 2026-09-17 |
| 13 | 0009 could not be fully honoured: `deriveAvailability` needs per-purpose `coverage`, only Tier 2 produces it, Tier 2 is not in the live path. **Settled by ruling 0013** — ship the registry half, label it honestly, track the rest as item 16. | 2026-09-16 |
| 14 | Build's finding: the live path already implements obtainability at `research-actions.ts:599` while `lib/research.ts:332` asks no availability question at all. Verified in the decision space, carried into ruling 0013 as the pattern to follow. | 2026-09-16 |
| 12 | The build space had no "nothing binds until it is in a file" rule, so build-side findings went to chat and the user carried them by hand. **Done** — `ROLE-build.md` now carries it, extended to findings outside the authorized work. | 2026-09-16 |
| 11 | Build verified rulings 0008 and 0010 independently rather than accepting them. No dispute. Recorded because a countercheck that agrees is still a countercheck. | 2026-09-16 |
| 7 | Where confirmation of a reconstructed ruling is recorded. **Settled by ruling 0012.** Implementation queued as item 10. | 2026-09-16 |
| 3 | Whether the availability ledger's screening default scope is right. **Settled by ruling 0011**: the narrowness is correct, the default is the defect. | 2026-09-16 |

## Format

`## Authorized now` takes `- key: value` lines. `ruling` is a ruling id or
`none`; the remaining keys follow the handoff shape — objective, approved
decision, scope, exclusions, acceptance checks, dependencies, unresolved,
report.

Open items are a table; `owner` is `decision` or `build`; `status` is one of the
six states in ruling 0015 (`proposed`, `approved`, `implemented`, `tested`,
`released`, `verified`), and decision items may only use the first two. Anything
at `tested` or beyond names its evidence in the row. If parts of an item sit at
different states, it is two items.

Move an item to Closed rather than deleting it — what was decided and then
dropped is itself a fact worth keeping. Compress on the way out: full reasoning
lives in the ruling, and this file is read every turn by both spaces.
