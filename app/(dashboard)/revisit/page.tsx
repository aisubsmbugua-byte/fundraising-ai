import { colors } from "@/lib/ui";

export default function RevisitPage() {
  return (
    <div style={{ maxWidth: 640 }}>
      <h1>Follow-up</h1>
      <p style={{ color: colors.textMuted, fontSize: 14 }}>
        Not built yet. This will hold relationship memory — the full interaction history with each
        funder, including past "no"s tracked as data rather than dead ends, so nothing has to be
        rediscovered from scratch.
      </p>
    </div>
  );
}
