import { colors } from "@/lib/ui";

export default function EvidencePage() {
  return (
    <div style={{ maxWidth: 640 }}>
      <h1>Evidence Library</h1>
      <p style={{ color: colors.textMuted, fontSize: 14 }}>
        Not built yet. This will hold verifiable outcomes and case studies with permission tags, used
        to ground AI-drafted outreach and proposals in real evidence instead of generic claims — see
        Slice 6.
      </p>
    </div>
  );
}
